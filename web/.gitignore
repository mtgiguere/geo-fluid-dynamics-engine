node_modules/
dist/
.env.local
test-results/
playwright-report/

# public/data/ IS committed, deliberately: it is the deterministic output of
# contract-tested code over cited public sources, and CI cannot fetch the
# inputs itself (the MIT returns file sits behind a manual guestbook).
# Regenerate with: uv run python scripts/export_web_data.py
