{
  "name": "geofluid-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "mapbox-gl": "^3.8.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "typescript": "~7.0.2",
    "vite": "^6.0.0",
    "vitest": "^4.1.8"
  }
}
