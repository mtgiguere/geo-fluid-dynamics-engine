# Mapbox PUBLIC token (pk.*) — client-side by design, but still: use a token
# URL-restricted to your deployment domains in the Mapbox dashboard, and keep
# the real value in .env.local (gitignored), never committed.
VITE_MAPBOX_TOKEN=pk.your-public-token-here
